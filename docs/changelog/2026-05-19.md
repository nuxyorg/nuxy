# Changelog - 2026-05-19

## Added

- `docs/DOCUMENTATION.md` — implementation status index.
- `docs/changelog/2026-05-19.md` (this file).
- `src/electron/config/permissions.ts` — manifest `permissions` enforcement at host boundary.
- `src/electron/ipc/broker.ts` — cross-extension invoke with `caller` / `callable` checks.
- `src/electron/ipc/worker-invoke.ts` — shared worker IPC helper.
- `extensions/shell/` — bootstrap launcher UI (`com.nuxy.shell`).
- `extensions/calculator/safe-eval.js` — safe math parser (no `eval`).
- `packages/ext-template/` — extension author starter template.
- `.github/workflows/ci.yml` — `pnpm test` + `pnpm -C src build`.
- `src/e2e/kernel-channels.spec.ts` — Playwright kernel validation smoke tests.

## Changed

- `packages/core` — `permissions`, `bootstrap`, `ExtensionRuntimeMeta`, `HostChannel.BROKER_INVOKE`, `core.extensions.invoke`.
- `packages/extension-host` — registry sync message, IPC channel tracking, broker proxy.
- `src/electron/extensions/registry.ts` — IPC allowlist, runtime merge, bootstrap helpers.
- `src/electron/ipc/validate.ts` — channel allowlist + `listOrchestrators` kernel channel.
- `src/electron/spawn/host-handlers.ts` — permission gate + broker host channel.
- `src/electron/spawn/spawn.ts` — handles `registry:sync` from workers.
- `src/renderer/App.tsx` — slim kernel loader (~90 lines); shell extension owns launcher UX.
- `extensions/clipboard/manifest.json` — `permissions: ["clipboard", "storage"]`.
- `extensions/calculator/backend.js` — uses `safeEvalMath`.
- `agents.md`, docs `00–19`, `implementation/*` — paths updated to `~/.nuxy`.
- `docs/21-extension-access.md` — statuses updated for implemented security/shell features.
- `docs/README.md` — link to DOCUMENTATION.md.
- Root `package.json` — removed unused `bun run start` script.

## Tests

- 42 kernel unit tests (permissions, broker, registry sync, safe-eval).
- Playwright e2e for kernel channel validation.

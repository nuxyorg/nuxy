# Changelog — 2026-06-21

## `angrysearch` — port to current extension architecture

- Ported the legacy `angrysearch` file-search extension (Voidtools Everything-style, SQLite FTS4-backed) from the old React + hooks architecture into a fresh `extensions/angrysearch/` implementation that follows the current rules (no React, controller + LitElement, light DOM, `tests/` layout).
- New files: `manifest.json`, `backend.ts`, `controller.ts`, `nuxy-tool-angrysearch.ts`, `frontend.ts`, `types.ts`, `settings.json`, `locales/{en,tr,ja}.json`, `tests/backend.test.ts`, `tests/controller.test.ts`, `tests/nuxy-tool-angrysearch.test.ts`.
- Preserved behavior: search-as-you-type with a 3-character minimum and 150ms debounce, LIKE vs. REGEXP (via a custom SQLite `REGEXP` function) query modes, configurable search result limit, periodic background re-indexing on a configurable interval, "open file" / "open containing folder" actions, and the ignored-roots safety list that prevents the indexer from descending into `/proc`, `/dev`, `/sys`, `/snap`, `/run`, `/tmp`, `/var/run`, `/var/lock` when scanning from `/`.
- Replaced the four legacy hooks (`useAngrysearchData`, `useAngrysearchActions`, `useAngrysearchKeyboard`, `useAngrysearchSync`) and the `AngrysearchItem.tsx` component with a single `AngrysearchController` class (state, IPC, keyboard actions via `window.core.shell.registerShellActions`) and a light-DOM `LitElement` (`nuxy-tool-angrysearch`) rendering a single-panel results list with `nuxy-list`/`nuxy-list-item`/`nuxy-empty-state`.
- Manifest permissions trimmed to the minimal real set actually used by the backend: `fs`, `db`, `shell` (the legacy manifest also declared `storage`, which the backend never called).
- Verified `DbHandle.function()` (used to register the custom `REGEXP` SQLite function) and FTS4 virtual tables are both supported by the current `core.db` runtime (`packages/extension-host/src/core-proxy.ts` opens a `node:sqlite` `DatabaseSync`, whose native `.function()` and `fts4` support were confirmed directly) — **no changes to `packages/core` or the runtime were needed**, since the type and implementation already existed.
- `pnpm -C src test`, `pnpm typecheck`, and `pnpm lint:fix` all pass with zero errors in the new files (44/44 new tests green; the only failing tests in the full suite are 3 pre-existing, unrelated failures in `download-manager`, `notes`, and `ui-default/Kbd`).

## `clipboard` extension — ported from legacy architecture

- Added `extensions/clipboard/` as a full rewrite of the legacy React-based clipboard manager extension, targeting the current LitElement + controller + `CoreContext` architecture (see `rules/EXTENSION_GUIDE.md`, `rules/LIT_MIGRATION_GUIDE.md`, `rules/FRONTEND_STRUCTURE_GUIDE.md`).
- `backend.ts` — unchanged behavior from the legacy backend: clipboard polling (text + image, configurable interval), history persisted via `core.storage`, pin/unpin, delete, clear, copy-to-clipboard (text/image), copy-file-to-clipboard with file-existence check, max-unpinned-items cap, `storeImages` setting toggle. Logic ported almost verbatim, since the `CoreContext` surface (`core.clipboard.*`, `core.storage.*`, `core.settings.*`, `core.fs.fileExists`) already matched the legacy SDK shape — no `packages/core` changes were needed.
- `controller.ts` — new `ClipboardController extends BaseExtensionController`, merging the legacy `useClipboardHistory` (polling refresh), `useClipboardActions` (copy/pin/unpin/delete with optimistic state), `useClipboardKeyboard` (now `registerShellActions`-based footer/menu actions, following the `notes`/`nyaa` pattern instead of the deprecated `useToolKeyActions` hook), `useOmniBarSync` (now `core.shell.controlOmniBar`), and `useSelectedItemMeta` (image dimension probe + file-existence probe) into one class.
- `nuxy-tool-clipboard.ts` — LitElement, light DOM, `<nuxy-two-panel>` list (left) + preview/properties (right), mirroring the `nyaa` extension's two-panel pattern. File-not-found alert renders via `<nuxy-alert variant="danger">` instead of a hand-rolled banner. Pin indicator, file/url/image leading icons, and color/image/text preview all preserved from the legacy `ClipboardLeftPanel`/`ClipboardRightPanel`/`ClipboardPreview`/`ClipboardItemLeading`/`FileIconFor` components, rebuilt as Lit templates using `window.UI` components (`nuxy-list`, `nuxy-list-item`, `nuxy-item-leading`, `nuxy-properties-panel`, `nuxy-icon`, `nuxy-alert`, `nuxy-empty-state`) instead of React/JSX.
- `utils/history.ts` and `utils/itemType.ts` ported essentially unchanged (pure logic, no `window.UI` dependency) — same for their test suites.
- `frontend.ts` reduced to a one-line bootstrap (`import './nuxy-tool-clipboard.ts'`).
- Settings (`pollIntervalMs`, `maxHistoryItems`, `storeImages`) and locales (`en`, `tr`, `ja`) ported from the legacy extension, with an added `search.placeholder` key for the new search-placeholder integration.
- No prior `clipboard` extension existed in this repo's git history — nothing to reconcile with `task/download-manager` or other prior work.
- Manifest: `com.nuxy.clipboard`, type `tool`, permissions `["clipboard", "storage", "fs"]` (matches actual `core.clipboard.*`, `core.storage.*`, `core.fs.fileExists` usage).
- Tests: `tests/history.test.ts`, `tests/itemType.test.ts`, `tests/backend.test.ts` (ported, all passing), plus new `tests/controller.test.ts` and `tests/nuxy-tool-clipboard.test.ts` for the new controller/element layer. Full `pnpm -C src test`, `pnpm typecheck`, `pnpm lint`, and `pnpm format` are green.

## `ollama` extension — ported from legacy architecture

- Added `extensions/ollama/` — a full rewrite of the legacy React-based Ollama chat extension (`/ollama` in the old extension generation) onto the current LitElement + controller architecture.
- `backend.ts` — `register(core)` exposes `chat`, `query`, `models`, `health`, `configure`, `getConfig`, `history:save/load/clear`, plus `eval`/`openWithQuery` for omnibar provider integration. Registers both `registerTool` and `registerProvider`, matching the `calculator`/`notes` provider convention. Settings (`host`, `model`, `thinkingColor`, `systemPrompt`, `temperature`) are read/written via `core.settings`, never `core.storage`. `core.storage` is used only for `history.json`. `fetch` is used directly against `${host}/api/chat` and `${host}/api/tags` — confirmed available as a permission-gated global in `packages/extension-host/src/index.ts` (requires the `network` permission, already declared).
- `controller.ts` — `OllamaController extends BaseExtensionController<OllamaState>`, merging the legacy `useOllamaData`/`useOllamaActions`/`useOllamaKeyboard`/`useOllamaSync` hooks into one class: loads config/history/models on connect, sends chat via a streaming `fetch` to `/api/chat` (`stream: true`), supports message queueing while a response is in flight, and registers footer/menu key actions (`Enter` send/queue, `Escape` stop, clear-history and model-switch menu entries) via `window.core.shell.registerShellActions`.
- `nuxy-tool-ollama.ts` — LitElement, light DOM (`createRenderRoot() { return this }`), renders chat history with the existing `nuxy-chat-message` UI-kit component, `nuxy-empty-state` for the zero-message state, and `nuxy-alert` for errors/queued-message notices. No custom input — text arrives via the `query` property setter from the omnibar, matching the no-own-input rule.
- `frontend.ts` is a one-line bootstrap (`import './nuxy-tool-ollama.ts'`).
- Backend uses `core.i18n.t()` for all provider-facing strings instead of the legacy hardcoded `"Ask Ollama"` / `"ollama — \"...\""` literals; locales (`en`, `tr`, `ja`) cover the new `provider.*` keys plus the original UI strings.
- Simplified relative to the legacy version: the `thinkingColor` setting is preserved and still configurable, but the gradient/window-border "thinking" animation (`nuxy-gradient-toggle` custom event) was intentionally **not** wired up, since that event is not in the documented approved `window.dispatchEvent` channel list (`rules/EXTENSION_GUIDE.md` §5.11) and is only otherwise used internally by the `gradient` helper extension's legacy bridge.
- Tests: `tests/backend.test.ts` (23 cases, ported 1:1 from the legacy spec plus new system-prompt/temperature coverage), `tests/controller.test.ts` (streaming chat, HTTP failure, blank-input guard, queueing), `tests/nuxy-tool-ollama.test.ts` (manifest tag resolution, query/committedQuery/extensionId property forwarding, connect/disconnect lifecycle). All 32 new tests pass; `pnpm typecheck` and `pnpm exec eslint extensions/ollama` are clean.


## `com.nuxy.store` — ported the legacy extension marketplace to the current architecture

- Full rewrite of the old React-based `store` extension (`hooks/useStoreData.ts`, `useStoreActions.ts`,
  `useStoreDerivedData.ts`, `useStoreKeyboard.ts`, `useStoreSync.tsx`, `components/StoreExtensionList.tsx`,
  `components/StoreExtensionDetail.tsx`) into `extensions/store/` using the current LitElement +
  controller architecture (light DOM, `@nuxyorg/core` `html`, `BaseExtensionController`,
  `ShellAction[]` keyboard wiring via `core.shell.registerShellActions`).
- New files: `manifest.json`, `backend.ts`, `controller.ts`, `frontend.ts`, `types.ts`, `settings.json`,
  `locales/{en,tr,ja}.json`, `utils/storeFilter.ts`, `tests/{backend,controller,storeFilter,nuxy-tool-store}.test.ts`.
- Behavior preserved from the legacy extension: remote registry JSON fetch (configurable `registryUrl`
  setting, default `https://raw.githubusercontent.com/atagulalan/nuxy-assets/main/registry.json`),
  merge with locally installed extensions, tab/search filtering (`all` / `tool` / `theme` / `iconpack` /
  `installed` / `updates`), install/update/uninstall actions, system-extension detection
  (`com.nuxy.shell`, `com.nuxy.settings`, and any extension with `manifest.bootstrap: true` are
  flagged `isSystem` and protected from uninstall in the UI).
- Behavior adjusted for the current UI kit: legacy `Badge` variants (`danger`/`warning`/`success`/`default`)
  mapped to the current `nuxy-tag` variant set (`red`/`orange`/`green`/`default`); legacy sidebar tab
  bar (`TabBar` + custom two-panel nav hook) replaced with `<nuxy-tab-bar orientation="vertical">` +
  `<nuxy-two-panel>`, driven entirely by the controller instead of React state.

### Kernel-dependency verification (important — read before assuming this needs follow-up work)

Verified by grepping `src/electron/` before writing `backend.ts`: kernel-side support for
`core.extensions.invoke('kernel', 'listInstalledExtensions' | 'installExtension' | 'uninstallExtension')`
**already exists and is fully wired** today:

- `src/electron/ipc/broker.ts` routes `targetId === 'kernel'` calls from worker extensions to
  `src/electron/ipc/kernel-invokable.ts` (`callKernelChannel`), which is real, no stub.
- `src/electron/ipc/kernel-invokable.ts` implements `listInstalledExtensions` (returns
  `loadedExtensions`), `installExtension` (delegates to `kernelInstallExtension`), and
  `uninstallExtension` (delegates to `kernelUninstallExtension`) — both defined in
  `src/electron/ipc/extension-ops.ts` with real filesystem behavior (downloads `.nuxyext`, writes to
  `EXTENSION_DIR`, triggers a rescan; uninstall removes the extension's folder/zip and rejects
  `com.nuxy.shell` / `com.nuxy.settings` / bootstrap extensions).
- All three channels are covered by existing kernel-side tests (`kernel-invokable.test.ts`,
  `extension-ops.test.ts`, `kernel-handlers/extensions.test.ts`, `broker.test.ts`, `register.test.ts`,
  `validate.test.ts`).

No kernel-side gap exists for this port — `backend.ts`'s `installExtension` / `uninstallExtension` /
`getExtensions` handlers are full proxies to real, already-shipped kernel functionality, not
placeholders.

### Test status

`pnpm -C src test`, `pnpm typecheck`, `pnpm lint`, and `pnpm format` all pass for the new
`extensions/store/` files (61 new tests, all green). Three pre-existing unrelated failures remain in
`download-manager`, `notes`, and `ui-default` `Kbd` tests (string/timing mismatches unrelated to this
change, reproducible on a clean checkout with no `extensions/store/` files present).

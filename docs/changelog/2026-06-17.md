# 2026-06-17

## DOM manipulation cleanup

- Replaced imperative DOM in ui-default components with Lit templates, real slots, and `<nuxy-portal>` (modal, select-box, tabs, conversion-card, dropdown-menu, table, callout, button, checkbox, switch, tooltip, etc.).
- Added shared utilities: `focus-trap.ts`, `parse-options.ts`; extended `scan-extensions.js` with DOM anti-pattern baseline and ESLint guardrails for extensions.
- Shell: `nativeInput` on command palette, `commandPaletteInput` ref, removed `querySelector` fallbacks in frontend/sync-controller/settings.
- Extension controllers: icon-browser footer via Lit ref; nyaa omni portal host owned by tool element.

## DOM manipulation documentation and lint rules

- Added Development Guide §5.13, anti-pattern W, checklist item, and [DOM Manipulation Rules](/extensions/dom-manipulation) doc page.
- Extended ESLint `no-restricted-syntax` on `extensions/**/*.ts`: `outerHTML`, `insertAdjacentHTML`, `replaceChildren`, `querySelector`, `this.appendChild`; shared `DOM_MANIPULATION_IGNORES` allowlist.
- Updated [Extension Linter](/extensions/linting) page with current ESLint rules; expanded `LIT_MIGRATION_GUIDE.md` and `AGENTS.md` §7.

## DOM lint warning cleanup

- Replaced remaining imperative DOM in ui-default (avatar-group, file-input, list, markdown-editor, media-preview, pin-input, select-box, tabs, truncate, two-panel), shell (frontend `@query` + `customElements.get` tool host, Lit portal-host), gradient viewmodel, and settings (`nativeInput` only).
- ESLint DOM rules now report zero warnings; allowlist trimmed to infrastructure-only paths (`tool-host`, `portal`, `render-markdown`, `scroll-into-view`, `gradient/gradient.ts`).
- File pick moved from `file-transfer/controller.ts` to Lit `<input type="file">` in `nuxy-tool-file-transfer.ts`; copy-button and gradient layer refactored off ignore list.
